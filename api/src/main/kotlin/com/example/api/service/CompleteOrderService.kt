package com.example.api.service

import org.springframework.stereotype.Service

@Service
class CompleteOrderService {
    fun completeOrder() {
        // order 완료 요청이 들어온다.
        println("completeOrder")

        // order 상태를 완료로 변경한다. 시간을 변경한다.

        // esl 이미지 쏜다.

        // 묶여있는 esl의 이미지를 변경한다 ( 앞으로 하나씩 당겨야 함 )
    }
}
